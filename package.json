{
  "name": "wishmaker-calc",
  "description": "Web app to calculate when to save for a shiny Wishmaker",
  "version": "0.1.0",
  "author": "Zaksabeast",
  "dependencies": {
    "lodash-es": "^4.17.10",
    "preact": "8.2.5",
    "preact-compat": "3.17.0"
  },
  "devDependencies": {
    "if-env": "^1.0.0",
    "less": "^2.7.2",
    "less-loader": "^4.0.5",
    "node-sass": "^4.5.3",
    "preact-cli": "^1.4.1",
    "sass-loader": "^6.0.6",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.1"
  },
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  }
}
